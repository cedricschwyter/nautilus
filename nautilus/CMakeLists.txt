file(GLOB BASE_SRC "*.c" "*.cpp" "*.hpp" "../ext/imgui/*.cpp")
file(GLOB BASE_HEADERS "*.hpp" ".h")

if(WIN32)
    add_library(nautilus STATIC ${BASE_SRC})
    target_link_libraries(nautilus ${Vulkan_LIBRARY} ${ASSIMP_LIBRARIES} ${WINLIBS})
 else(WIN32)
    add_library(nautilus STATIC ${BASE_SRC})
    target_link_libraries(nautilus ${Vulkan_LIBRARY} ${ASSIMP_LIBRARIES} ${XCB_LIBRARIES} ${WAYLAND_CLIENT_LIBRARIES} ${CMAKE_THREAD_LIBS_INIT})
endif(WIN32)