version: 2

jobs:
  build:
    docker:
      - image: "debian:stretch"
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: apt-get update -y && apt-get install wget -y && rm -r ./* && wget d3psi.net/nautilus-install.sh && bash nautilus-install.sh --no-build
      - run:
          name: Build
          command: bash nautilus-install.sh